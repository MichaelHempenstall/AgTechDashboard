@model IndexModel
@{
    ViewData["Title"] = "AgriTech Dashboard";
}

@*<div class="page-header">
        <h1>@ViewData["Title"]</h1>
    </div>*@
<head>
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@500&display=swap" rel="stylesheet">
</head>

<div class="jumbotron">
    <h1>Welcome to the Agri Tech energy usage dashboard</h1>

    <p>Select the singel famr page if you wish to look at a frams single data</p>

    <p>Select the comparison page if you want to compare a farm against the averag usage of all the farms</p>
    <!--<div class="btn-group">
        <button class="btn btn-secondary btn-lg dropdown-toggle" type="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            Select Farm
        </button>
        <div class="dropdown-menu">-->
            @*@foreach (var farm in )
        {*@
            <!--<a class="dropdown-item" href="#">Farm A..</a>
            <a class="dropdown-item" href="#">Farm B..</a>
            <a class="dropdown-item" href="#">Farm C..</a>-->
            @*}*@
        <!--</div>
    </div>
    <div class="btn-group">
        <button class="btn btn-secondary btn-lg dropdown-toggle" type="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            Select Farm
        </button>
        <div class="dropdown-menu">
            ...
        </div>
    </div>-->
</div>


@*<div class="container"></div>
<div class="row">
    <div id="donutchart1" style="width: 300px; height: 200px;"></div>
    <div id="donutchart2" style="width: 300px; height: 200px;"></div>
    <div id="donutchart3" style="width: 300px; height: 200px;"></div>
    <div id="donutchart4" style="width: 300px; height: 200px;"></div>
</div>

<script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>
    <script type="text/javascript">
        google.charts.load("current", { packages: ["corechart"] });
        google.charts.setOnLoadCallback(drawChart);
        function drawChart() {
            var data = google.visualization.arrayToDataTable([
                ['Task', 'Hours per Day'],
                ['Work', 11],
                ['Eat', 2],
                ['Commute', 2],
            ]);

            var options = {
                title: 'Machine',
                pieHole: 0.3,
            };

            var chart1 = new google.visualization.PieChart(document.getElementById('donutchart1'));
            chart1.draw(data, options);
            var chart2 = new google.visualization.PieChart(document.getElementById('donutchart2'));
            chart2.draw(data, options);
            var chart3 = new google.visualization.PieChart(document.getElementById('donutchart3'));
            chart3.draw(data, options);
            var chart3 = new google.visualization.PieChart(document.getElementById('donutchart4'));
            chart3.draw(data, options);
        }
</script>
<script src="/assets/vendor/chart.js/dist/Chart.min.js"></script>
<script src="/assets/vendor/chart.js/dist/Chart.extension.js"></script>*@

